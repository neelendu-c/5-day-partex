tags:

parameters:
  - name: page
    in: query
    type: integer
    required: false
    description: The page number

responses:
  200:
    description: Showing documents
    schema:
      id: return_greet
      properties:
        result:
          type: string
          description: The test
          default: test