tags:

parameters:
  - name: option
    in: query
    type: integer
    required: false
    description: 1 for ascending,-1 for descending
  - name: sortval
    in: query
    type: string
    required: false
    description: Field to sort by

responses:
  200:
    description: Sort room in ascending or descending order by chosen field
    schema:
      id: return_test
      properties:
        result:
          type: string
          description: The test
          default: test