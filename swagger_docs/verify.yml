tags:

parameters:
  - name: token
    in: query
    type: string
    required: false
    description: Token from client

responses:
  200:
    description: Token
    schema:
      id: return_test
      properties:
        result:
          type: string
          description: The test
          default: test