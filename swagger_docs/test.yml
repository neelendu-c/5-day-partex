tags:

parameters:
  - name: size
    in: query
    type: integer
    required: false
    description: Size of elements
  - name: X-API-Key
    in: query
    type: string
    required: false
    description: Size of elements

responses:
  200:
    description: A single user item
    schema:
      id: return_test
      properties:
        result:
          type: string
          description: The test
          default: test